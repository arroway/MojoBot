A simple web interface for the LolBot module using the Mojolicious web framework.

Requires (at the moment) HTTP::Table::FromDatabase to diplay the data from the database created by the LolBot.

/!\ Static path to fetch the database /!\
-> modify lib/MojoBot/Display.pm line 10


Launch with: perl script/mojobot.pl daemon --reload 
